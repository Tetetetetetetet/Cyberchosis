// using UnityEngine;
//using UnityEngine.UI;
//public class HeroHealthController : MonoBehaviour
//{
    //public Slider HealthSlider; 
    //public Text HealthText;
    //public GameObject target;

    //private float maxHealth;
    //private float currentHealth;
    //// Start is called before the first frame update
    //void Start()
    //{
        //maxHealth = target.GetComponent<CharacterHealth>().maxHealth;
        //currentHealth = target.GetComponent<CharacterHealth>().currentHealth;
        //UpdateHealthBar();
    //}

    //// Update is called once per frame
    //void Update()
    //{
        //currentHealth = target.GetComponent<CharacterHealth>().currentHealth;
        //UpdateHealthBar();
    //}
    //public void UpdateHealthBar(){
        //float HealthPercent = currentHealth / maxHealth;
        //HealthSlider.value = HealthPercent;
        //HealthText.text = currentHealth.ToString("0") + "/" + maxHealth.ToString("0");
    //}
//}
